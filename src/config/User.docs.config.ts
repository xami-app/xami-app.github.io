import { ConfigDefinition } from "../types/Config.types";

const userDocConfig: ConfigDefinition = {
    basePath: "/docs/user/",
    title: "User",
    entries: [
        {
            title: "Watching content",
            path: "watch-content",
        }
    ]
};

export default userDocConfig;